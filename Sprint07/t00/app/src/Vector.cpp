// #include "Vector.h"

// CBL::Vector<T>::Vector(size_t size):  {
    
// }
// CBL::Vector<>::Vector(size_t size) {

// // }
// template<class T> 
// CBL::Vector<T>::Vector() {

// }


// template<class T> 
// CBL::Vector<T>::~Vector() {
//     if (m_capacity > 0)
//         delete [] m_buffer;
// }

// template<class T> 
// CBL::Vector<T>::Vector(size_t size) : m_size(size), m_capacity(size) {
//     m_buffer = new T[size];
// }

// template<class T>
// size_t CBL::Vector<T>::size() const {
//     return m_size();
// }

// template<class T>
// size_t CBL::Vector<T>::capacity() const {
//     return m_capacity();
// }
